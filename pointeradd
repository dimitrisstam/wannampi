#include <stdio.h>

int main()
{
	int num1, num2, sum;
	int *ptr1, *ptr2;
	
	ptr1 = &num1;
	ptr2 = &num2;
	
	printf("Enter two numbers:\n");
	scanf("%d%d", &num1, &num2);
	
	sum = *ptr1 + *ptr2;
	printf("%d", sum);
	
	return 0;
}
